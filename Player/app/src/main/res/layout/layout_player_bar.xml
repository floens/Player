<?xml version="1.0" encoding="utf-8"?>
<org.floens.player.layout.PlayerBar xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/music_bar"
    android:layout_width="match_parent"
    android:layout_height="@dimen/music_bar_height"
    android:background="#ffffffff"
    android:elevation="6dp"
    android:orientation="horizontal">

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:orientation="vertical"
        android:paddingLeft="48dp">

        <TextView
            android:id="@+id/title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_horizontal|top"
            android:maxLines="1"
            android:paddingTop="3dp"
            android:text="@string/dummy_title"
            android:textColor="?attr/file_title_text_color"
            android:textSize="?attr/file_title_text_size" />

        <TextView
            android:id="@+id/description"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_horizontal|bottom"
            android:maxLines="1"
            android:paddingBottom="4dp"
            android:text="@string/dummy_description"
            android:textColor="?attr/file_description_text_color"
            android:textSize="?attr/file_description_text_size" />

    </LinearLayout>

    <org.floens.player.view.ReactiveButton
        android:id="@+id/button_play_pause"
        android:layout_width="48dp"
        android:layout_height="match_parent" />

</org.floens.player.layout.PlayerBar>
